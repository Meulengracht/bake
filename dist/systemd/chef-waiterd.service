[Unit]
Description=Service for chefs remote orchestrator
Wants=network.target
After=network.target

[Service]
ExecStart=@CMAKE_INSTALL_FULL_LIBEXECDIR@/chef/waiterd -vvv
Restart=on-failure
Type=simple
